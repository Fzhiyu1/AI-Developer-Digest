version: "3.8"

services:
  daily-paper:
    build: .
    container_name: ai-daily-paper
    env_file: .env
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
    volumes:
      - .:/app
      - ~/.ssh:/home/appuser/.ssh:ro
    restart: unless-stopped
